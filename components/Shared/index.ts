
import { styled } from "baseui";

export const CalendarContainer = styled('div', () => {
    return {
      display:"flex", 
      width:'100%',
      justifyContent:'center',
      alignContent:'center',
      alignItems:'center',
    }
  })